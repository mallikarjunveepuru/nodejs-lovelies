extend layout

block content
  h1 Input Songs
  section#input
    form(method="POST", action="/song")
      input(type="text", name="title", placeholder="Song title")
      input(type="text", name="artist", placeholder="Song artist")
      input(type="submit", value="POST")
  include ./partials/setlist
